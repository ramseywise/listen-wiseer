from pydantic import BaseSettings

# isort: off

# class ModelingSettings(BaseSettings):
#     """General scoring settings."""
#
#     features_types: tuple[type[Features]] = (ApplicationOverdraftFeatures,)
#     model_types: tuple[type[Model[Any, Any, Any]]] = (ApplicationOverdraftModelV01,)
#
#
# modeling_settings = ModelingSettings()
